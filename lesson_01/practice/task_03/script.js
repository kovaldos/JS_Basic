const C = 10;
const D = 2;

const SUM = C + D;
const DIFF = C - D;
const MULT = C * D;
const DIVIDE = C / D;

console.log(C, D, SUM, DIFF, MULT, DIVIDE);
console.log(C + D + SUM + DIFF + MULT + DIVIDE);




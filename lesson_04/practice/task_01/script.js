// 1.

const numbers = [1, 2, 3]
numbers.forEach((number) => console.log(number));

// 2.

const randomArr = ['string', 2, true];

console.log(randomArr.length)

// 3.

let arr = ['a', 'b', 'c'];

// Прямое обращение к элементам массива для изменения значений
arr[0] = 1;
arr[1] = 2;
arr[2] = 3;

// Выводим измененный массив в консоль
console.log(arr);



